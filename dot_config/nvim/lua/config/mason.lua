require("mason").setup({
    ui = {
        icons = {
            package_installed = "[INSTALLED]",
            package_pending = "[PENDING]",
            package_uninstalled = "[UNINSTALLED]"
        }
    }
})
